; PlatformIO configuration for ESP32-C3 Game Controller
; USB HID (Keyboard + Mouse) via native USB

[env:esp32c3]
platform = espressif32
board = esp32-c3-devkitc-02
framework = arduino

; Enable USB HID
build_flags =
    -DARDUINO_USB_MODE=1
    -DARDUINO_USB_CDC_ON_BOOT=0

monitor_speed = 115200
